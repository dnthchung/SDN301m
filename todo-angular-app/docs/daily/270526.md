# Daily Log - 27/05/2026

**Author:** ChungDT

**Task List:**

- [Task 1: Xây dựng ứng dụng todo Angular](#task-1)
- [Task 2: Thêm JSON Server mock REST API](#task-2)
- [Task 3: Thêm shared header và các trang điều hướng](#task-3)
- [Task 4: Thêm authentication bằng session](#task-4)
- [Task 5: Mở rộng phân quyền admin user guest](#task-5)
- [Task 6: Chuyển users sang JSON Server](#task-6)
- [Task 7: Thêm trang 404](#task-7)

---

## Task 1

- Date: 27/05/2026
- Task name (short): Xây dựng ứng dụng todo Angular
- Short description: Xây dựng ứng dụng todo đơn giản để học Angular 21.2.x, chưa dùng NgRx hoặc RxJS cho state. UI được triển khai theo phong cách GitHub với danh sách task, filter, thống kê và thao tác thêm/sửa/xoá.
- What changed: Tạo feature `todos` theo standalone component, dùng `signal()` và `computed()` cho state local, cấu hình lazy route bằng `loadComponent`, thay placeholder Angular CLI bằng app shell gọn, thêm style GitHub-like, cập nhật unit test và ignore log local của dev server.
- Main files: `src/app/features/todos/todo-page/todo-page.ts`, `src/app/features/todos/todo-page/todo-page.html`, `src/app/features/todos/todo-page/todo-page.scss`, `src/app/features/todos/models/todo.model.ts`, `src/app/app.routes.ts`, `src/app/app.ts`, `src/app/app.html`, `src/app/app.scss`, `src/styles.scss`, `src/app/app.spec.ts`, `src/app/features/todos/todo-page/todo-page.spec.ts`, `.gitignore`
- Problems: `npm run build` compile/prerender thành công nhưng fail ở bước ghi output do file cũ `dist/todo-angular-app/3rdpartylicenses.txt` bị Windows từ chối xoá với lỗi `EPERM`. Project cũng chưa có script `lint`.
- Solution: Chạy build kiểm chứng bằng output path riêng `npx ng build --output-path .angular/build-check` và kết quả pass; chạy `npx ng test --watch=false` pass; chạy Prettier check cho các file đã sửa pass. Lint được ghi nhận là chưa áp dụng vì thiếu script.
- Notes: Build output riêng vẫn có warning component style budget 4KB cho `todo-page.scss`; warning không chặn compile. Cần xử lý quyền thư mục `dist` hoặc xoá artifact cũ bằng quyền phù hợp trước khi dùng lại `npm run build` mặc định.

## Task 2

- Date: 27/05/2026
- Task name (short): Thêm JSON Server mock REST API
- Short description: Áp dụng JSON Server vào dự án để mô phỏng RESTful API cho todo. Component todo chuyển từ dữ liệu hard-code sang gọi API mock với các thao tác tải, thêm, sửa, xoá và xoá các task đã hoàn thành.
- What changed: Cài `json-server`, thêm script `server:mock`, tạo `db.json`, thêm `TodoApiService` dùng `fetch`/`async` cho CRUD, cập nhật component dùng service kết hợp signals cho UI state, thêm trạng thái loading/error, cập nhật unit test mock service và bổ sung README hướng dẫn chạy mock API.
- Main files: `package.json`, `package-lock.json`, `db.json`, `README.md`, `src/app/features/todos/models/todo.model.ts`, `src/app/features/todos/services/todo-api.service.ts`, `src/app/features/todos/todo-page/todo-page.ts`, `src/app/features/todos/todo-page/todo-page.html`, `src/app/features/todos/todo-page/todo-page.scss`, `src/app/features/todos/todo-page/todo-page.spec.ts`
- Problems: `npm run build` vẫn fail ở bước xoá file cũ trong `dist` do lỗi quyền Windows `EPERM`; project chưa có script `lint`. Component style vẫn vượt warning budget 4KB nhưng không chặn compile.
- Solution: Chạy `npx ng build --output-path .angular/build-check` để xác nhận source build/prerender pass; chạy `npx ng test --watch=false` pass; chạy Prettier check pass; khởi động `npm run server:mock` và kiểm tra `GET http://127.0.0.1:3000/todos` trả 3 records.
- Notes: Khi phát triển cần chạy 2 terminal: `npm run server:mock` cho REST mock và `npm start` cho Angular app. Nếu muốn `npm run build` mặc định pass hoàn toàn, cần xử lý quyền hoặc xoá thư mục `dist` bằng user có quyền phù hợp.

## Task 3

- Date: 27/05/2026
- Task name (short): Thêm shared header và các trang điều hướng
- Short description: Thêm header dùng chung giữa các component để điều hướng đến trang chủ Todo, trang Profile và trang Users. Trang Users có giao diện quản lý user cơ bản để học routing, shared component và feature page.
- What changed: Tạo `AppHeader` standalone component dùng `RouterLink` và `RouterLinkActive`, gắn header vào root app shell, thêm route lazy-load cho `/profile` và `/users`, tạo Profile page, tạo Users page với signal local cho search/filter/thống kê, bổ sung model user và cập nhật unit test.
- Main files: `src/app/shared/components/app-header/app-header.ts`, `src/app/shared/components/app-header/app-header.html`, `src/app/shared/components/app-header/app-header.scss`, `src/app/shared/components/app-header/app-header.spec.ts`, `src/app/app.ts`, `src/app/app.html`, `src/app/app.routes.ts`, `src/app/app.spec.ts`, `src/app/features/profile/profile-page/profile-page.ts`, `src/app/features/profile/profile-page/profile-page.html`, `src/app/features/profile/profile-page/profile-page.scss`, `src/app/features/profile/profile-page/profile-page.spec.ts`, `src/app/features/users/models/user.model.ts`, `src/app/features/users/users-page/users-page.ts`, `src/app/features/users/users-page/users-page.html`, `src/app/features/users/users-page/users-page.scss`, `src/app/features/users/users-page/users-page.spec.ts`
- Problems: `npm run build` vẫn fail ở bước xoá file cũ trong `dist` do lỗi quyền Windows `EPERM`; project chưa có script `lint`. Todo stylesheet vẫn vượt warning budget component style 4KB.
- Solution: Chạy `npx ng test --watch=false` pass 13 tests; chạy `npx ng build --output-path .angular/build-check` pass và prerender 3 route; chạy Prettier check pass. Lint ghi nhận là chưa áp dụng vì thiếu script.
- Notes: Không tự start dev server sau task này để developer tự chạy local theo yêu cầu trước đó.

## Task 4

- Date: 27/05/2026
- Task name (short): Thêm authentication bằng session
- Short description: Thiết lập cụm chức năng đăng nhập, đăng ký và session cho ứng dụng học Angular. Session dùng `sessionStorage`, có hạn 5 phút; đăng nhập bằng email/mật khẩu, đăng ký thêm username.
- What changed: Tạo core auth model/service/guard, thêm Sign in page, Sign up page, cập nhật route `/sign-in` và `/sign-up`, bảo vệ `/profile` và `/users`, cập nhật shared header để hiện username khi đã đăng nhập hoặc hai nút Sign up/Sign in khi chưa đăng nhập, bổ sung unit test cho auth pages và header.
- Main files: `src/app/core/auth/auth.model.ts`, `src/app/core/auth/auth.service.ts`, `src/app/core/auth/auth.guard.ts`, `src/app/features/auth/sign-in-page/sign-in-page.ts`, `src/app/features/auth/sign-in-page/sign-in-page.html`, `src/app/features/auth/sign-in-page/sign-in-page.scss`, `src/app/features/auth/sign-in-page/sign-in-page.spec.ts`, `src/app/features/auth/sign-up-page/sign-up-page.ts`, `src/app/features/auth/sign-up-page/sign-up-page.html`, `src/app/features/auth/sign-up-page/sign-up-page.scss`, `src/app/features/auth/sign-up-page/sign-up-page.spec.ts`, `src/app/shared/components/app-header/app-header.ts`, `src/app/shared/components/app-header/app-header.html`, `src/app/shared/components/app-header/app-header.scss`, `src/app/shared/components/app-header/app-header.spec.ts`, `src/app/app.routes.ts`
- Problems: `npm run build` vẫn fail ở bước xoá file cũ trong `dist` do lỗi quyền Windows `EPERM`; project chưa có script `lint`. Todo stylesheet vẫn vượt warning budget component style 4KB.
- Solution: Chạy `npx ng test --watch=false` pass 19 tests; chạy `npx ng build --output-path .angular/build-check` pass và prerender 5 route; chạy Prettier check pass. Lint ghi nhận là chưa áp dụng vì thiếu script.
- Notes: Auth hiện là mock client-side để học tập, không dùng cho production vì registered users đang lưu trong `localStorage`. Không tự start dev server sau task này để developer tự chạy local.

## Task 5

- Date: 27/05/2026
- Task name (short): Mở rộng phân quyền admin user guest
- Short description: Mở rộng dự án theo ba vai trò guest, user và admin. Guest chỉ ở trang chủ, user chỉ dùng todo cá nhân, admin xem tổng quan hệ thống và quản lý user.
- What changed: Thêm role `admin/user` vào auth model và session, tạo role guard dùng `route.data.roles`, đổi trang chủ thành Home page cho guest, chuyển Todo sang route `/todos` chỉ dành cho user, thêm Admin Dashboard route `/admin` cho totals, đổi Users page thành màn quản lý registered users, cập nhật header hiển thị navigation theo role, thêm `ownerId` vào todo để mỗi user có danh sách riêng.
- Main files: `src/app/core/auth/auth.model.ts`, `src/app/core/auth/auth.service.ts`, `src/app/core/auth/auth.guard.ts`, `src/app/app.routes.ts`, `src/app/shared/components/app-header/app-header.ts`, `src/app/shared/components/app-header/app-header.html`, `src/app/features/home/home-page/home-page.ts`, `src/app/features/home/home-page/home-page.html`, `src/app/features/admin/admin-dashboard-page/admin-dashboard-page.ts`, `src/app/features/admin/admin-dashboard-page/admin-dashboard-page.html`, `src/app/features/users/users-page/users-page.ts`, `src/app/features/users/users-page/users-page.html`, `src/app/features/todos/models/todo.model.ts`, `src/app/features/todos/services/todo-api.service.ts`, `src/app/features/todos/todo-page/todo-page.ts`, `db.json`
- Problems: `npm run build` vẫn fail ở bước xoá file cũ trong `dist` do lỗi quyền Windows `EPERM`; project chưa có script `lint`. Todo stylesheet vẫn vượt warning budget component style 4KB.
- Solution: Chạy `npx ng test --watch=false` pass 23 tests; chạy `npx ng build --output-path .angular/build-check` pass và prerender 6 route; chạy Prettier check pass. Lint ghi nhận là chưa áp dụng vì thiếu script.
- Notes: Admin/user hiện được chọn ở Sign up để tiện học tập. Đây là client-side mock authorization nên không dùng cho production; backend thật vẫn cần kiểm tra quyền ở server.

## Task 6

- Date: 27/05/2026
- Task name (short): Chuyển users sang JSON Server
- Short description: Chuẩn hoá dữ liệu user để không còn lưu registered users trong `localStorage`. Users được đưa vào `db.json` và auth gọi JSON Server qua REST API.
- What changed: Thêm resource `users` vào `db.json`, seed admin/user demo, chuyển `AuthService` sang dùng `GET /users`, `POST /users`, `PATCH /users/:id`, `DELETE /users/:id`, giữ session 5 phút trong `sessionStorage`, cập nhật Sign in/Sign up và Users page theo async service.
- Main files: `db.json`, `src/app/core/auth/auth.service.ts`, `src/app/features/auth/sign-in-page/sign-in-page.ts`, `src/app/features/auth/sign-in-page/sign-in-page.spec.ts`, `src/app/features/auth/sign-up-page/sign-up-page.ts`, `src/app/features/auth/sign-up-page/sign-up-page.spec.ts`, `src/app/features/users/users-page/users-page.ts`, `src/app/features/users/users-page/users-page.spec.ts`
- Problems: `npm run build` vẫn fail ở bước xoá file cũ trong `dist` do lỗi quyền Windows `EPERM`; project chưa có script `lint`. Todo stylesheet vẫn vượt warning budget component style 4KB.
- Solution: Chạy `npx ng test --watch=false` pass 23 tests; chạy `npx ng build --output-path .angular/build-check` pass và prerender 6 route; chạy Prettier check pass. Lint ghi nhận là chưa áp dụng vì thiếu script.
- Notes: Khi chạy local cần bật `npm run server:mock` trước để auth có thể đọc/ghi `/users`. Tài khoản seed: `admin@example.com/admin123` và `user@example.com/user123`.

## Task 7

- Date: 27/05/2026
- Task name (short): Thêm trang 404
- Short description: Thêm trang Not Found cho các route không tồn tại. Trang có thông báo 404 và nút Back để quay lại trang trước đó.
- What changed: Tạo `NotFoundPage` standalone component, thêm giao diện 404 theo style GitHub-like, thêm nút `Back` dùng `Location.back()`, thêm link fallback về Home, đổi wildcard route `**` từ redirect sang lazy-load trang 404 và bổ sung unit test.
- Main files: `src/app/features/not-found/not-found-page/not-found-page.ts`, `src/app/features/not-found/not-found-page/not-found-page.html`, `src/app/features/not-found/not-found-page/not-found-page.scss`, `src/app/features/not-found/not-found-page/not-found-page.spec.ts`, `src/app/app.routes.ts`
- Problems: `npm run build` vẫn fail ở bước xoá file cũ trong `dist` do lỗi quyền Windows `EPERM`; project chưa có script `lint`. Todo stylesheet vẫn vượt warning budget component style 4KB.
- Solution: Chạy `npx ng test --watch=false` pass 26 tests; chạy `npx ng build --output-path .angular/build-check` pass và prerender 6 route; chạy Prettier check pass. Lint ghi nhận là chưa áp dụng vì thiếu script.
- Notes: Nút Back phụ thuộc browser history; nếu không có history thì người dùng vẫn có link `Go home` để quay về trang chủ.
