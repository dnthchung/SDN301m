export enum HttpStatusCode {
  ERROR = 404,
  SUCCESS = 200,
}

export enum HttpMessage {
  ERROR = "Server response error roi",
  SUCCESS = "Server response success roi",
}

export enum Status {
  ACTIVE = "active",
  INACTIVE = "inactive",
  DELETED = "deleted",
}

export enum Genre {
  Action = "Action",
  Drama = "Drama",
  Comedy = "Comedy",
  Cartoon = "Cartoon",
}
